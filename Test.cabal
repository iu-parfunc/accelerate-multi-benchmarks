-- Initial Test.cabal generated by cabal init.  For further documentation, 
-- see http://haskell.org/cabal/users-guide/

-- The name of the package.
name:                Test

-- The package version.  See the Haskell package versioning policy (PVP) 
-- for standards guiding when and how versions should be incremented.
-- http://www.haskell.org/haskellwiki/Package_versioning_policy
-- PVP summary:      +-+------- breaking API changes
--                   | | +----- non-breaking API additions
--                   | | | +--- code changes with no API change
version:             0.1.0.0

-- A short (one-line) description of the package.
-- synopsis:            

-- A longer description of the package.
-- description:         

-- The license under which the package is released.
license:             BSD3

-- The file containing the license text.
license-file:        LICENSE

-- The package author(s).
author:              Joel Svensson

-- An email address to which users can send suggestions, bug reports, and 
-- patches.
maintainer:          svenssonjoel@yahoo.se

-- A copyright notice.
-- copyright:           

-- category:            

build-type:          Simple

-- Extra files to be distributed with the package, such as examples or a 
-- README.
extra-source-files:  README.md

-- Constraint on the version of Cabal needed to build this package.
cabal-version:       >=1.10


executable Test
  -- .hs or .lhs file containing the Main module.
  main-is:             Test.hs
  
  ghc-options: -threaded 
  -- Modules included in this executable, other than Main.
  -- other-modules:       
  
  -- LANGUAGE extensions used by modules in this package.
  other-extensions:    BangPatterns, ScopedTypeVariables, TypeOperators, ExplicitForAll, RankNTypes, CPP, PatternGuards, Rank2Types, TypeSynonymInstances, FlexibleInstances, MagicHash, FlexibleContexts, PackageImports, ParallelListComp, DeriveDataTypeable, MultiParamTypeClasses, TypeFamilies, UndecidableInstances, ViewPatterns, OverloadedStrings, TemplateHaskell, NoMonomorphismRestriction, ForeignFunctionInterface, ExistentialQuantification, TupleSections, GADTs, StandaloneDeriving, DefaultSignatures, RecordWildCards, UnboxedTuples, IncoherentInstances, NoForeignFunctionInterface, GeneralizedNewtypeDeriving, QuasiQuotes, ImpredicativeTypes, OverlappingInstances, NoImplicitPrelude, ConstraintKinds, OverloadedLists, NamedFieldPuns, EmptyDataDecls, FunctionalDependencies, KindSignatures, PolyKinds, InstanceSigs, AllowAmbiguousTypes, DeriveGeneric, EmptyCase, LambdaCase
  
  -- Other library packages from which modules are imported.
  build-depends:       base >=4.7 && <4.8, accelerate >=2.0 && <2.1, OpenGL >=2.10 && <2.11, GLUT >=2.6 && <2.7, containers >=0.5 && <0.6, repa >=3.3 && <3.4, repa-io >=3.3 && <3.4, vector >=0.10 && <0.11, ghc-prim >=0.3 && <0.4, random >=1.1 && <1.2, bmp >=1.2 && <1.3, bytestring >=0.10 && <0.11, directory >=1.2 && <1.3, filepath >=1.3 && <1.4, fclabels >=2.0 && <2.1, criterion >=1.0 && <1.1, ekg >=0.4 && <0.5, mwc-random >=0.13 && <0.14, cereal >=0.4 && <0.5, bytestring-lexing >=0.4 && <0.5, QuickCheck >=2.7 && <2.8, parallel >=3.2 && <3.3, array >=0.5 && <0.6, mtl >=2.1 && <2.2, deepseq >=1.3 && <1.4, pretty >=1.1 && <1.2, hashtables >=1.2 && <1.3, vector-algorithms >=0.6 && <0.7, attoparsec >=0.12 && <0.13, primitive >=0.5 && <0.6, test-framework >=0.8 && <0.9, test-framework-hunit >=0.3 && <0.4, test-framework-quickcheck2 >=0.3 && <0.4, HUnit >=1.2 && <1.3, unix >=2.7 && <2.8, old-locale >=1.0 && <1.1, transformers >=0.3 && <0.4, unordered-containers >=0.2 && <0.3, binary >=0.7 && <0.8, hashable >=1.2 && <1.3, cryptohash >=0.11 && <0.12, process >=1.2 && <1.3, mainland-pretty >=0.2 && <0.3, text >=1, SafeSemaphore >=0.10 && <0.11, old-time >=1.1 && <1.2, language-c-quote >=0.10 && <0.11, template-haskell >=2.9 && <2.10, srcloc >=0.4 && <0.5, cufft >=0.1 && <0.2, symbol >=0.2 && <0.3, time >=1.4 && <1.5, aeson >=0.8 && <0.9, statistics >=0.13 && <0.14, ansi-wl-pprint >=0.6 && <0.7, network >=2.6 && <2.7, MonadRandom >=0.3 && <0.4, normaldistribution >=1.1 && <1.2, xml >=1.3 && <1.4, case-insensitive >=1.2 && <1.3, unix-compat >=0.4 && <0.5, regex-posix >=0.95 && <0.96, accelerate-cuda >= 2.0.0.0 
  
  -- Directories containing source files.
  -- hs-source-dirs:      
  
  -- Extra tools (e.g. alex, hsc2hs, ...) needed to build the source.
  build-tools:         c2hs, hsc2hs
  
  -- Base language which the package is written in.
  default-language:    Haskell2010
  
executable Test4
  -- .hs or .lhs file containing the Main module.
  main-is:             Test4.hs
  
  ghc-options: -threaded 
  -- Modules included in this executable, other than Main.
  -- other-modules:       
  
  -- LANGUAGE extensions used by modules in this package.
  other-extensions:    BangPatterns, ScopedTypeVariables, TypeOperators, ExplicitForAll, RankNTypes, CPP, PatternGuards, Rank2Types, TypeSynonymInstances, FlexibleInstances, MagicHash, FlexibleContexts, PackageImports, ParallelListComp, DeriveDataTypeable, MultiParamTypeClasses, TypeFamilies, UndecidableInstances, ViewPatterns, OverloadedStrings, TemplateHaskell, NoMonomorphismRestriction, ForeignFunctionInterface, ExistentialQuantification, TupleSections, GADTs, StandaloneDeriving, DefaultSignatures, RecordWildCards, UnboxedTuples, IncoherentInstances, NoForeignFunctionInterface, GeneralizedNewtypeDeriving, QuasiQuotes, ImpredicativeTypes, OverlappingInstances, NoImplicitPrelude, ConstraintKinds, OverloadedLists, NamedFieldPuns, EmptyDataDecls, FunctionalDependencies, KindSignatures, PolyKinds, InstanceSigs, AllowAmbiguousTypes, DeriveGeneric, EmptyCase, LambdaCase
  
  -- Other library packages from which modules are imported.
  build-depends:       base >=4.7 && <4.8, accelerate >=2.0 && <2.1, OpenGL >=2.10 && <2.11, GLUT >=2.6 && <2.7, containers >=0.5 && <0.6, repa >=3.3 && <3.4, repa-io >=3.3 && <3.4, vector >=0.10 && <0.11, ghc-prim >=0.3 && <0.4, random >=1.1 && <1.2, bmp >=1.2 && <1.3, bytestring >=0.10 && <0.11, directory >=1.2 && <1.3, filepath >=1.3 && <1.4, fclabels >=2.0 && <2.1, criterion >=1.0 && <1.1, ekg >=0.4 && <0.5, mwc-random >=0.13 && <0.14, cereal >=0.4 && <0.5, bytestring-lexing >=0.4 && <0.5, QuickCheck >=2.7 && <2.8, parallel >=3.2 && <3.3, array >=0.5 && <0.6, mtl >=2.1 && <2.2, deepseq >=1.3 && <1.4, pretty >=1.1 && <1.2, hashtables >=1.2 && <1.3, vector-algorithms >=0.6 && <0.7, attoparsec >=0.12 && <0.13, primitive >=0.5 && <0.6, test-framework >=0.8 && <0.9, test-framework-hunit >=0.3 && <0.4, test-framework-quickcheck2 >=0.3 && <0.4, HUnit >=1.2 && <1.3, unix >=2.7 && <2.8, old-locale >=1.0 && <1.1, transformers >=0.3 && <0.4, unordered-containers >=0.2 && <0.3, binary >=0.7 && <0.8, hashable >=1.2 && <1.3, cryptohash >=0.11 && <0.12, process >=1.2 && <1.3, mainland-pretty >=0.2 && <0.3, text >=1, SafeSemaphore >=0.10 && <0.11, old-time >=1.1 && <1.2, language-c-quote >=0.10 && <0.11, template-haskell >=2.9 && <2.10, srcloc >=0.4 && <0.5, cufft >=0.1 && <0.2, symbol >=0.2 && <0.3, time >=1.4 && <1.5, aeson >=0.8 && <0.9, statistics >=0.13 && <0.14, ansi-wl-pprint >=0.6 && <0.7, network >=2.6 && <2.7, MonadRandom >=0.3 && <0.4, normaldistribution >=1.1 && <1.2, xml >=1.3 && <1.4, case-insensitive >=1.2 && <1.3, unix-compat >=0.4 && <0.5, regex-posix >=0.95 && <0.96, accelerate-cuda >= 2.0.0.0 
  
  -- Directories containing source files.
  -- hs-source-dirs:      
  
  -- Extra tools (e.g. alex, hsc2hs, ...) needed to build the source.
  build-tools:         c2hs, hsc2hs
  
  -- Base language which the package is written in.
  default-language:    Haskell2010
  
